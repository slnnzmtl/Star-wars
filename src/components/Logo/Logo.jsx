import React from 'react';
import logo from '../../../public/logo.png';

const Logo = () => (
  <div className="logo">
    <img src={logo} alt="" className="logo__image" />
  </div>
);

export default Logo;
